% Document contains all the global settings for the work

% Header and footer
\fancyhf{} % sets both header and footer to nothing
\renewcommand{\headrulewidth}{0pt}
\pagestyle{fancy}
\fancyhead[L]{\fontsize{11}{14}\selectfont RTU.STUDPROJ.BAKATBSIST.PPS}
\fancyhead[R]{\fontsize{11}{14}\selectfont PROGRAMMATŪRAS PRASĪBU SPECIFIKĀCIJA}
\fancyfoot[L]{Versija: 1.0, 01.06.2023}
\fancyfoot[R]{Lappuse \thepage. no \pageref{LastPage}}

% Header and footer for the ToC
\fancypagestyle{plain}{%
  \fancyhf{}
  \renewcommand{\headrulewidth}{0pt}
  \fancyhead[L]{\fontsize{11}{14}\selectfont RTU.STUDPROJ.BAKATBSIST.PPS}
  \fancyhead[R]{\fontsize{11}{14}\selectfont PROGRAMMATŪRAS PRASĪBU SPECIFIKĀCIJA}
  \fancyfoot[L]{Versija: 1.0, 01.06.2023}
  \fancyfoot[R]{Lappuse \thepage. no \pageref{LastPage}}
}
\setlength{\headheight}{14.0pt}

% Set main language as Latvian for this document
\setmainlanguage{latvian}
\setdefaultlanguage{latvian}
\setotherlanguages{english}

% Add dots to the title numbers
\titlelabel{\thetitle.\quad}

% space lining is 1.5
\renewcommand{\baselinestretch}{1.5}

% Setting the main font for the work to Times New Roman
\setmainfont{Times New Roman}

% Page numbering is Arabic 12th font
% \pagenumbering{arabic}
% https://tex.stackexchange.com/questions/3731/how-to-change-the-formatting-font-of-page-numbers
% \fancypagestyle{plain}{%
%     \fancyhf{} % clear all header and footer fields
%     \fancyhead[R]{\normalfont\fontsize{20pt}{14pt}\thepage}}
% % \renewcommand{\headrulewidth}{0pt}
% % \renewcommand{\footrulewidth}{0pt}} % except the center

% Set new section styling
% Set spacings <command> <left> <before> <after>
\titleformat*{\section}
{\fontsize{16pt}{16pt}\centering\bfseries}
\titlespacing*{\section}{0pt}{0pt}{24pt}

\titleformat*{\subsection}
{\fontsize{14pt}{16pt}\centering\bfseries}
\titlespacing*{\subsection}{0pt}{12pt}{12pt}

\titleformat*{\subsubsection}
{\fontsize{12pt}{14pt}\centering\bfseries}
\titlespacing*{\subsubsection}{0pt}{12pt}{12pt}

% Set indent length
\setlength\parindent{1.27cm}

% Caption settings for images
\captionsetup[figure]{name={Attēls}}
\captionsetup[figure]{font=normalsize,font=bf}
\captionsetup{labelfont=bf, font=bf}
% Remove colon
\captionsetup[figure]{labelsep=space}

% Caption settings for tables
\captionsetup[table]{name={tabula}}
\captionsetup[table]{labelsep=space}

% Turn on numbering per section for figures
% \numberwithin{figure}{section}

% Add section number to the equation numbering
\numberwithin{equation}{section}
% Set one more dot to the equation numbering
\renewcommand{\theequation}{\thesection.\arabic{equation}.}

% Spacings between a caption and a figure
% \setlength{\abovecaptionskip}{6pt}
% \setlength{\belowcaptionskip}{12pt}

% Create a new macro for figure references
% https://tex.stackexchange.com/questions/381532/custom-reference-types-e-g-freffigure-1-renders-fig-1
\newcommand{\fref}[1]{Attēls \ref{#1}.}

% Create a new macro for table references
\newcommand{\tref}[1]{\ref{#1}.tabula}

% Create a new macro for equation references
\newcommand{\eref}[1]{(\ref{#1})}

% Apply settings to minted
% Set a font as described in the book
\setmonofont{Arial Narrow}

% \renewcommand{\footrulewidth}{0pt}
% Turn off indentation
\setlength{\parindent}{0cm}